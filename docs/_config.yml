title: Keel MySQL Extension
description: Documentation for Keel MySQL Extension
remote_theme: pages-themes/merlot@v0.2.0
plugins:
  - jekyll-remote-theme

# Site settings
lang: zh-CN
show_downloads: true
google_analytics:

# Navigation (if supported by theme or manually in templates)
# Merlot theme is simple and might not have a built-in sidebar navigation, 
# but it follows the standard README.md / index.md structure.
